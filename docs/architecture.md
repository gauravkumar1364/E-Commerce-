# Architecture Overview

This project is organized as a React + Vite frontend, a Flask API backend, and a MySQL database layer.

The database layer is normalized around identity, catalog, commerce, and security domains. The schema and ER diagram live in [database/schema.sql](../database/schema.sql) and [database/README.md](../database/README.md).